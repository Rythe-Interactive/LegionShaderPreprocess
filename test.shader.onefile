4295002929
262
#version 450 
#define LEGION_ENGINE 
#define VERTEX_SHADER
io struct pipeline {
vec3 pos;
vec3 normal;
};
layout(location=0) in vec3 pos;
layout(location=SV_NORMAL) in vec3 normal;
void main()
{
gl_Position = pos;
pipeline.pos = pos;
pipeline.normal = normal;
}
8589970224
180
#version 450 
#define LEGION_ENGINE 
#define FRAGMENT_SHADER
io struct pipeline {
vec3 pos;
vec3 normal;
};
out vec4 color;
void main()
{
vec4(pipeline.pos + pipeline.normal,1);
}
